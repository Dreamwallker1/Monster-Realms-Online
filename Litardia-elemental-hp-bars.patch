From 9c4cd5f2d96cb05c1406ef805f699f530c2b34fc Mon Sep 17 00:00:00 2001
From: Codex <codex@openai.com>
Date: Sat, 1 Aug 2026 18:05:04 +0100
Subject: [PATCH] Move elemental health bars above combatants

---
 .../src/components/game/BattleOverlay.tsx     | 122 +++++++++---------
 artifacts/monster-realms/src/index.css        |  39 +++++-
 2 files changed, 98 insertions(+), 63 deletions(-)

diff --git a/artifacts/monster-realms/src/components/game/BattleOverlay.tsx b/artifacts/monster-realms/src/components/game/BattleOverlay.tsx
index b9bd6fe..470d350 100644
--- a/artifacts/monster-realms/src/components/game/BattleOverlay.tsx
+++ b/artifacts/monster-realms/src/components/game/BattleOverlay.tsx
@@ -715,6 +715,7 @@ function HpPlate({
   const pct = Math.max(0, Math.min(100, (currentHp / maxHp) * 100));
   const [lagPct, setLagPct] = useState(pct);
   const elColors = getElementColors(element);
+  const isFire = element.toLowerCase() === 'fire';
   const barColor = pct > 50 ? '#22C55E' : pct > 20 ? '#EAB308' : '#EF4444';
   const qualLabel = QUALITY_LABEL[rarity] ?? rarity;
 
@@ -727,7 +728,7 @@ function HpPlate({
 
   return (
     <div
-      className={`rounded-xl px-4 py-3 battle-hp-card ${pct <= 20 ? 'battle-hp-critical' : ''}`}
+      className={`rounded-xl px-4 py-3 battle-hp-card ${isFire ? 'battle-hp-card-fire' : ''} ${pct <= 20 ? 'battle-hp-critical' : ''}`}
       style={{
         background: 'linear-gradient(135deg, rgba(8,8,22,0.92), rgba(4,4,14,0.96))',
         border: `2px solid ${elColors.primary}66`,
@@ -759,28 +760,37 @@ function HpPlate({
         <b>HP</b><span>{Math.round(pct)}%</span>
       </div>
       {/* HP bar — numbers live inside the bar */}
-      <div
-        className="relative w-full rounded-full overflow-hidden battle-vital-bar"
-        style={{ background: 'rgba(0,0,0,0.72)', border: '1px solid rgba(255,255,255,0.16)' }}
-      >
-        <div className="absolute inset-y-0 left-0 rounded-full battle-hp-lag" style={{ width: `${lagPct}%` }} />
+      <div className="battle-hp-meter-wrap">
         <div
-          className="absolute inset-y-0 left-0 rounded-full hp-bar-fill"
-          style={{
-            width: `${pct}%`,
-            background: `linear-gradient(90deg, ${barColor}88, ${barColor}dd)`,
-            boxShadow: `0 0 6px ${barColor}66`,
-          }}
-        />
-        {/* HP text overlay */}
-        <div className="absolute inset-0 flex items-center justify-center pointer-events-none">
-          <span
-            className="text-xs font-mono font-black leading-none"
-            style={{ color: 'rgba(255,255,255,0.92)', textShadow: '0 1px 4px rgba(0,0,0,0.98)' }}
+          className={`relative w-full rounded-full overflow-hidden battle-vital-bar ${isFire ? 'battle-vital-fire' : ''}`}
+          style={{ background: 'rgba(0,0,0,0.72)', border: '1px solid rgba(255,255,255,0.16)' }}
+        >
+          <div className="absolute inset-y-0 left-0 rounded-full battle-hp-lag" style={{ width: `${lagPct}%` }} />
+          <div
+            className={`absolute inset-y-0 left-0 rounded-full hp-bar-fill ${isFire ? 'battle-fire-hp-fill' : ''}`}
+            style={{
+              width: `${pct}%`,
+              background: isFire ? undefined : `linear-gradient(90deg, ${barColor}88, ${barColor}dd)`,
+              boxShadow: isFire ? undefined : `0 0 6px ${barColor}66`,
+            }}
           >
-            {currentHp}<span style={{ opacity: 0.5 }}>/{maxHp}</span>
-          </span>
+            {isFire && <><i /><i /><i /><i /><i /></>}
+          </div>
+          {/* HP text overlay */}
+          <div className="absolute inset-0 flex items-center justify-center pointer-events-none">
+            <span
+              className="text-xs font-mono font-black leading-none"
+              style={{ color: 'rgba(255,255,255,0.96)', textShadow: '0 1px 4px rgba(0,0,0,0.98)' }}
+            >
+              {currentHp}<span style={{ opacity: 0.58 }}>/{maxHp}</span>
+            </span>
+          </div>
         </div>
+        {isFire && pct < 99 && (
+          <div className="battle-fire-ash" style={{ left: `${pct}%`, width: `${100 - pct}%` }} aria-hidden="true">
+            <i /><i /><i /><i /><i /><i /><i /><i />
+          </div>
+        )}
       </div>
       {statusEffect && <div className="battle-status-chip">{statusEffect}</div>}
     </div>
@@ -1634,30 +1644,6 @@ export default function BattleOverlay() {
         <div className="battle-stage-pool battle-stage-pool-player" aria-hidden="true" />
         <BattleBiomeScenery regionId={regionId} ambientColor={theme.ambientColor} />
 
-        {/* Wild HP plate — upper left */}
-        <div className="absolute battle-slide-up battle-hp-anchor battle-hp-anchor-left battle-hp-position-left" style={{ animationDelay: '0.75s' }}>
-          <HpPlate
-            name={wildMonster.species.name}
-            level={wildMonster.level}
-            element={wildMonster.species.element}
-            rarity={wildMonster.species.rarity}
-            currentHp={wildMonster.currentHp}
-            maxHp={wildMonster.maxHp}
-            align="left"
-            statusEffect={wildMonster.statusEffect}
-          />
-          {wildMonster.shinyVariant && (
-            <div className="mt-1 text-center text-[10px] font-bold" style={{ color: '#FFD700', textShadow: '0 0 8px gold' }}>
-              ✨ {wildMonster.shinyVariant} Shiny
-            </div>
-          )}
-          {wildHpPct < 30 && (
-            <div className="mt-1 text-center text-[10px] animate-pulse" style={{ color: '#34D399' }}>
-              ● Good catch chance!
-            </div>
-          )}
-        </div>
-
         {/* ── Unified turn widget — top centre ────────────────────────────── */}
         <div
           className="absolute top-3 left-1/2 -translate-x-1/2 battle-slide-up flex flex-col items-center"
@@ -1730,20 +1716,6 @@ export default function BattleOverlay() {
 
         </div>
 
-        {/* Player myth HP plate — upper right */}
-        <div className="absolute battle-slide-up battle-hp-anchor battle-hp-anchor-right battle-hp-position-right" style={{ animationDelay: '0.8s' }}>
-          <HpPlate
-            name={playerMonster.species.name}
-            level={playerMonster.level}
-            element={playerMonster.species.element}
-            rarity={playerMonster.species.rarity}
-            currentHp={playerMonster.currentHp}
-            maxHp={playerMonster.maxHp}
-            align="right"
-            statusEffect={playerMonster.statusEffect}
-          />
-        </div>
-
         {/* ── Combatants — depth-layered absolute positioning ──────────────── */}
 
         {/* Character — CENTER BACK (smaller, feet on ground) */}
@@ -1769,6 +1741,28 @@ export default function BattleOverlay() {
           className="absolute flex flex-col items-center battle-entrance battle-combatant-wild"
           style={{ animationDelay: '0.05s', zIndex: 2 }}
         >
+          <div className="battle-hp-over-myth battle-hp-over-myth-wild battle-slide-up" style={{ animationDelay: '0.75s' }}>
+            <HpPlate
+              name={wildMonster.species.name}
+              level={wildMonster.level}
+              element={wildMonster.species.element}
+              rarity={wildMonster.species.rarity}
+              currentHp={wildMonster.currentHp}
+              maxHp={wildMonster.maxHp}
+              align="left"
+              statusEffect={wildMonster.statusEffect}
+            />
+            {wildMonster.shinyVariant && (
+              <div className="mt-1 text-center text-[10px] font-bold" style={{ color: '#FFD700', textShadow: '0 0 8px gold' }}>
+                ✨ {wildMonster.shinyVariant} Shiny
+              </div>
+            )}
+            {wildHpPct < 30 && (
+              <div className="mt-1 text-center text-[10px] animate-pulse" style={{ color: '#34D399' }}>
+                ● Good catch chance!
+              </div>
+            )}
+          </div>
           {/* Stagger wrapper — re-keyed when wild takes a hit */}
           <div key={`wstagger-${wildShake}`} className={wildShake > 0 ? 'hit-stagger-right' : ''}>
           {/* Inner lunge wrapper — re-keyed on each wild attack to replay animation */}
@@ -1815,6 +1809,18 @@ export default function BattleOverlay() {
           className="absolute flex flex-col items-center battle-entrance battle-combatant-player"
           style={{ animationDelay: '0.1s', zIndex: 2 }}
         >
+          <div className="battle-hp-over-myth battle-hp-over-myth-player battle-slide-up" style={{ animationDelay: '0.8s' }}>
+            <HpPlate
+              name={playerMonster.species.name}
+              level={playerMonster.level}
+              element={playerMonster.species.element}
+              rarity={playerMonster.species.rarity}
+              currentHp={playerMonster.currentHp}
+              maxHp={playerMonster.maxHp}
+              align="right"
+              statusEffect={playerMonster.statusEffect}
+            />
+          </div>
           {/* Stagger wrapper — re-keyed when player myth takes a hit */}
           <div key={`pstagger-${playerShake}`} className={playerShake > 0 ? 'hit-stagger-left' : ''}>
           {/* Inner lunge wrapper — re-keyed on each player attack to replay animation */}
diff --git a/artifacts/monster-realms/src/index.css b/artifacts/monster-realms/src/index.css
index 8453f06..2d29141 100644
--- a/artifacts/monster-realms/src/index.css
+++ b/artifacts/monster-realms/src/index.css
@@ -533,9 +533,6 @@
     background:linear-gradient(90deg,transparent,rgba(255,181,113,.14),rgba(255,224,184,.42),rgba(255,181,113,.14),transparent);
     box-shadow:0 0 18px rgba(255,101,42,.25);
   }
-  .battle-hp-position-left { top: max(14px, env(safe-area-inset-top)); left: 16px; }
-  .battle-hp-position-right { top: max(14px, env(safe-area-inset-top)); right: 16px; }
-
   .battle-celestial {
     position: absolute; z-index: 0; left: 50%; top: 13%; width: clamp(120px, 17vw, 260px); aspect-ratio: 1;
     transform: translateX(-50%); border-radius: 50%; pointer-events: none;
@@ -681,12 +678,41 @@
   .battle-hp-anchor { transform: scale(1); z-index: 15; }
   .battle-hp-anchor-left { transform-origin: top left; }
   .battle-hp-anchor-right { transform-origin: top right; }
-  .battle-hp-card { position:relative; overflow:hidden; width:clamp(226px,23vw,290px); }
+  .battle-hp-over-myth { position:relative; z-index:16; margin-bottom:10px; pointer-events:none; }
+  .battle-hp-over-myth-wild { align-self:flex-start; }
+  .battle-hp-over-myth-player { align-self:flex-end; }
+  .battle-hp-card { position:relative; overflow:visible; width:clamp(226px,23vw,290px); }
   .battle-hp-card::before { content:""; position:absolute; inset:0; pointer-events:none; background:linear-gradient(115deg,rgba(255,255,255,.09),transparent 28% 72%,rgba(255,255,255,.035)); }
   .battle-hp-card::after { content:""; position:absolute; left:10%; right:10%; top:0; height:1px; background:linear-gradient(90deg,transparent,rgba(255,255,255,.5),transparent); opacity:.55; }
   .battle-hp-caption { display:flex; align-items:center; justify-content:space-between; margin:0 2px 4px; color:rgba(255,255,255,.72); font:800 9px var(--font-mono,monospace); letter-spacing:.13em; }
   .battle-hp-caption b { color:#fff; font-size:11px; text-shadow:0 0 8px rgba(255,255,255,.25); }
   .battle-vital-bar { height:28px; box-shadow:inset 0 2px 7px rgba(0,0,0,.8),0 1px 0 rgba(255,255,255,.1),0 0 14px rgba(34,197,94,.1); }
+  .battle-hp-meter-wrap { position:relative; isolation:isolate; }
+  .battle-hp-card-fire { border-color:rgba(251,92,36,.72) !important; box-shadow:0 8px 28px rgba(0,0,0,.82),0 0 18px rgba(249,74,20,.42) !important; }
+  .battle-vital-fire { background:linear-gradient(180deg,#211008,#070201) !important; box-shadow:inset 0 2px 8px #000,0 0 14px rgba(255,83,15,.34); }
+  .battle-fire-hp-fill {
+    overflow:visible; transform-origin:left center;
+    background:linear-gradient(180deg,#fff2a4 0 8%,#ffbf32 22%,#ff5b18 58%,#8e1609 100%);
+    box-shadow:0 0 7px #ffcc3d,0 0 16px rgba(255,73,10,.78),inset 0 -5px 8px rgba(92,8,3,.55);
+    transition:width .48s cubic-bezier(.2,.75,.25,1);
+  }
+  .battle-fire-hp-fill::before {
+    content:""; position:absolute; inset:-5px 0 0; opacity:.82;
+    background:
+      radial-gradient(ellipse at 12% 100%,#fff6ad 0 7%,#ff8b1f 9% 18%,transparent 21%),
+      radial-gradient(ellipse at 36% 100%,#ffd14c 0 8%,#ff4d12 11% 20%,transparent 23%),
+      radial-gradient(ellipse at 64% 100%,#fff09a 0 7%,#ff7718 10% 18%,transparent 22%),
+      radial-gradient(ellipse at 88% 100%,#ffd33d 0 8%,#f13b0b 11% 20%,transparent 23%);
+    background-size:34px 31px; animation:battle-fire-hp-flame .72s ease-in-out infinite alternate;
+  }
+  .battle-fire-hp-fill > i { position:absolute; z-index:2; bottom:3px; width:3px; height:3px; border-radius:50%; background:#fff2a0; box-shadow:0 0 5px #ff7a17; animation:battle-fire-hp-ember 1.05s ease-out infinite; }
+  .battle-fire-hp-fill > i:nth-child(1){left:12%;animation-delay:-.2s}.battle-fire-hp-fill > i:nth-child(2){left:31%;animation-delay:-.75s}.battle-fire-hp-fill > i:nth-child(3){left:52%;animation-delay:-.4s}.battle-fire-hp-fill > i:nth-child(4){left:72%;animation-delay:-.9s}.battle-fire-hp-fill > i:nth-child(5){left:91%;animation-delay:-.55s}
+  .battle-fire-ash { position:absolute; z-index:4; top:12px; height:42px; pointer-events:none; overflow:visible; }
+  .battle-fire-ash > i { position:absolute; top:0; width:3px; height:3px; border-radius:45% 55% 55% 45%; background:#a69e97; box-shadow:0 0 3px #4b4541; opacity:0; animation:battle-fire-ash-fall 1.45s linear infinite; }
+  .battle-fire-ash > i:nth-child(1){left:5%;animation-delay:-.2s}.battle-fire-ash > i:nth-child(2){left:18%;animation-delay:-.85s;width:4px}.battle-fire-ash > i:nth-child(3){left:33%;animation-delay:-1.2s}.battle-fire-ash > i:nth-child(4){left:47%;animation-delay:-.45s;width:2px}.battle-fire-ash > i:nth-child(5){left:61%;animation-delay:-1s}.battle-fire-ash > i:nth-child(6){left:74%;animation-delay:-.65s;width:4px}.battle-fire-ash > i:nth-child(7){left:87%;animation-delay:-1.35s}.battle-fire-ash > i:nth-child(8){left:96%;animation-delay:-.05s;width:2px}
+  @keyframes battle-fire-hp-flame { from{transform:translateY(2px) scaleY(.88);filter:saturate(1)} to{transform:translateY(-1px) scaleY(1.08);filter:saturate(1.2) brightness(1.08)} }
+  @keyframes battle-fire-hp-ember { 0%{opacity:0;transform:translate(0,3px) scale(.5)} 30%{opacity:1} 100%{opacity:0;transform:translate(5px,-18px) scale(.1)} }
+  @keyframes battle-fire-ash-fall { 0%{opacity:0;transform:translate(0,-4px) rotate(0)} 18%{opacity:.82} 100%{opacity:0;transform:translate(9px,34px) rotate(190deg)} }
   .battle-hp-lag { background:linear-gradient(90deg,#f4d86a,#fff0a2); opacity:.8; transition:width .72s cubic-bezier(.18,.72,.2,1); box-shadow:0 0 9px #ffe78388; }
   .battle-hp-critical { animation:battle-hp-critical 1.15s ease-in-out infinite alternate; }
   @keyframes battle-hp-critical { to { border-color:#ef444488; box-shadow:0 4px 24px rgba(0,0,0,.78),0 0 18px rgba(239,68,68,.3); } }
@@ -1068,7 +1094,8 @@
     .battle-hit-wild { left: 11%; }
     .battle-hit-player { right: 11%; }
     .battle-hp-anchor { scale:1; }
-    .battle-hp-card { width:min(42vw,190px); padding:8px 9px !important; }
+    .battle-hp-over-myth { transform:scale(1.34); transform-origin:bottom center; margin-bottom:18px; }
+    .battle-hp-card { width:min(39vw,190px); padding:8px 9px !important; }
     .battle-vital-bar { height:23px; }
     .battle-hp-caption { margin-bottom:3px; }
   }
@@ -1078,6 +1105,8 @@
     .battle-combatant-player { scale: .52; bottom: 2%; }
     .battle-combatant-wild { left: -5%; }
     .battle-combatant-player { right: -5%; }
+    .battle-hp-over-myth { transform:scale(1.62); margin-bottom:25px; }
+    .battle-hp-card { width:37vw; }
     .myth-lunge-left { --battle-lunge-x: -32vw; --battle-lunge-recoil: -30vw; }
     .myth-lunge-right { --battle-lunge-x: 32vw; --battle-lunge-recoil: 30vw; }
     .battle-stage-pool { width: 42vw; }
-- 
2.51.1

