From ec4726f6248fa844f51e6610ee77ff472fba0b23 Mon Sep 17 00:00:00 2001
From: Codex <codex@openai.com>
Date: Sat, 1 Aug 2026 14:07:59 +0300
Subject: [PATCH] Anchor Myth skills to authored limb actions

---
 .../src/__tests__/SkillCinematic.test.tsx     |  16 +++
 .../src/components/battle/SkillCinematic.tsx  | 117 +++++++-----------
 .../src/components/game/BattleOverlay.tsx     |  15 +--
 3 files changed, 68 insertions(+), 80 deletions(-)

diff --git a/artifacts/monster-realms/src/__tests__/SkillCinematic.test.tsx b/artifacts/monster-realms/src/__tests__/SkillCinematic.test.tsx
index 088a4f9..01d522e 100644
--- a/artifacts/monster-realms/src/__tests__/SkillCinematic.test.tsx
+++ b/artifacts/monster-realms/src/__tests__/SkillCinematic.test.tsx
@@ -51,3 +51,19 @@ describe('Flarelynx basic attack cinematic', () => {
     expect(screen.getAllByTestId('flarelynx-fire-claw-wave')).toHaveLength(2);
   });
 });
+
+describe('limb-authored skill delivery', () => {
+  it('launches Ashquill Ashen Gust from a charged limb as a projectile', () => {
+    render(<SkillCinematic skillName="Ashen Gust" element="Fire" power={62} attackerSide="wild"
+      attackerMythId="ashquill" attackerRarity="C" skillType="skill1" actionPhase="strike" onComplete={vi.fn()} />);
+    expect(screen.getAllByTestId('limb-charge')).toHaveLength(1);
+    expect(screen.getAllByTestId('limb-projectile')).toHaveLength(1);
+  });
+
+  it('keeps Flarelynx Magma Pounce physical without spawning a projectile', () => {
+    render(<SkillCinematic skillName="Magma Pounce" element="Fire" power={64} attackerSide="player"
+      attackerMythId="flarelynx" attackerRarity="C" skillType="skill1" actionPhase="strike" onComplete={vi.fn()} />);
+    expect(screen.getAllByTestId('limb-charge')).toHaveLength(1);
+    expect(screen.queryByTestId('limb-projectile')).not.toBeInTheDocument();
+  });
+});
diff --git a/artifacts/monster-realms/src/components/battle/SkillCinematic.tsx b/artifacts/monster-realms/src/components/battle/SkillCinematic.tsx
index ac18edc..7b86d64 100644
--- a/artifacts/monster-realms/src/components/battle/SkillCinematic.tsx
+++ b/artifacts/monster-realms/src/components/battle/SkillCinematic.tsx
@@ -181,36 +181,58 @@ function sides(side: 'player' | 'wild') {
     : { from: '25%', to: '75%', fromN: 25, toN: 75 };
 }
 
-function PhysicalSkillStage({ side, skillType, color, glow }: {
-  side: 'player' | 'wild'; skillType: string; color: string; glow: string;
+type SkillDelivery = 'melee' | 'projectile';
+type LimbProfile = { name: string; y: number };
+
+function getSkillDelivery(mythId?: string, skillType?: string): SkillDelivery {
+  const basic = !skillType || skillType === 'normal' || skillType === 'attack';
+  if (mythId === 'flarelynx') return basic || skillType === 'skill2' || skillType === 'ultimate' ? 'projectile' : 'melee';
+  if (mythId === 'ashquill') return skillType === 'skill1' || skillType === 'skill2' ? 'projectile' : 'melee';
+  return skillType === 'skill2' || skillType === 'ultimate' ? 'projectile' : 'melee';
+}
+
+function getLimbProfile(mythId?: string, skillType?: string): LimbProfile {
+  const basic = !skillType || skillType === 'normal' || skillType === 'attack';
+  if (mythId === 'flarelynx') {
+    if (basic || skillType === 'skill1') return { name: 'PAWS', y: 53 };
+    return { name: 'TAIL', y: 55 };
+  }
+  if (mythId === 'ashquill') {
+    if (basic) return { name: 'BEAK', y: 46 };
+    if (skillType === 'skill1' || skillType === 'skill2') return { name: 'WINGS', y: 43 };
+    return { name: 'TAIL', y: 54 };
+  }
+  return { name: 'CORE', y: 48 };
+}
+
+function PhysicalSkillStage({ side, skillType, color, glow, mythId }: {
+  side: 'player' | 'wild'; skillType: string; color: string; glow: string; mythId?: string;
 }) {
   const { from, to } = sides(side);
-  const style = skillType === 'skill1' ? 'leap' : skillType === 'skill2' || skillType === 'ultimate' ? 'burst' : 'combo';
-  if (style === 'leap') return (
+  const delivery = getSkillDelivery(mythId, skillType);
+  const limb = getLimbProfile(mythId, skillType);
+  const direction = side === 'player' ? -1 : 1;
+  if (delivery === 'melee') return (
     <>
-      <motion.div className="absolute w-24 h-24 rounded-full border-2" style={{ left: from, top: '52%', borderColor: color, boxShadow: `0 0 32px ${glow}` }}
-        initial={{ x: '-50%', y: '-10%', scale: .25, opacity: 0 }}
-        animate={{ y: ['-10%', '-230%', '-35%'], scale: [.25, 1.15, .65], opacity: [0, .9, .75, 0] }}
-        transition={{ duration: 1.25, ease: [.22,.72,.18,1], times: [0,.42,.78,1] }} />
+      <motion.div data-testid="limb-charge" className="absolute rounded-full border-2" style={{ left: from, top: `${limb.y}%`, width: 62, height: 62, x: '-50%', y: '-50%', borderColor: color, boxShadow: `0 0 32px ${glow}`, background: `radial-gradient(circle,#fff7d0 0 5%,${color}55 18%,transparent 68%)` }}
+        initial={{ scale: .2, opacity: 0 }} animate={{ scale: [.2,1.15,.68], opacity: [0,.95,0] }} transition={{ duration: .56, ease: 'easeOut' }} />
+      <motion.div className="absolute h-1 rounded-full" style={{ left: from, top: `${limb.y}%`, width: '26%', transformOrigin: side === 'player' ? 'right' : 'left', background: `linear-gradient(${side === 'player' ? '270deg' : '90deg'},transparent,${color})`, boxShadow: `0 0 18px ${glow}` }}
+        initial={{ scaleX: 0, opacity: 0 }} animate={{ x: direction * 120, scaleX: [0,1,.2], opacity: [0,.75,0] }} transition={{ duration: .72, delay: .18 }} />
       {[0, 1, 2].map((ring) => <motion.div key={ring} className="absolute rounded-full border-2" style={{ left: to, top: '64%', borderColor: ring === 0 ? '#fff2bd' : color, boxShadow: `0 0 22px ${glow}` }}
         initial={{ x: '-50%', y: '-50%', width: 8, height: 8, opacity: 0 }} animate={{ width: 90 + ring * 55, height: 24 + ring * 18, opacity: [0, .9, 0] }}
-        transition={{ duration: .9, delay: .92 + ring * .09, ease: 'easeOut' }} />)}
+        transition={{ duration: .9, delay: .62 + ring * .09, ease: 'easeOut' }} />)}
     </>
   );
-  if (style === 'burst') return (
+  return (
     <>
-      <motion.div className="absolute rounded-full" style={{ left: from, top: '48%', width: 84, height: 84, background: `radial-gradient(circle,#fff7cf,${color} 32%,transparent 70%)`, boxShadow: `0 0 58px ${glow}` }}
-        initial={{ x: '-50%', y: '-50%', scale: .08, opacity: 0 }} animate={{ scale: [.08,.55,1.18,.2], opacity: [0,.65,1,0] }} transition={{ duration: 1.55, times: [0,.35,.72,1] }} />
-      <motion.div className="absolute h-3 rounded-full" style={{ top: '48%', left: side === 'player' ? '20%' : '25%', width: '55%', transformOrigin: side === 'player' ? 'right' : 'left', background: `linear-gradient(90deg,transparent,${color},#fff8d0,${color},transparent)`, boxShadow: `0 0 34px ${glow}` }}
-        initial={{ scaleX: 0, opacity: 0 }} animate={{ scaleX: [0,0,1,.3], opacity: [0,0,1,0] }} transition={{ duration: 1.7, times: [0,.48,.7,1] }} />
-      <motion.div className="absolute rounded-full" style={{ left: to, top: '49%', width: 180, height: 220, background: `radial-gradient(circle,${color}88,${color}22 48%,transparent 72%)`, boxShadow: `0 0 70px ${glow}` }}
-        initial={{ x: '-50%', y: '-50%', scale: .1, opacity: 0 }} animate={{ scale: [.1,.1,1.25,.85], opacity: [0,0,1,0] }} transition={{ duration: 2.05, times: [0,.48,.7,1] }} />
+      <motion.div data-testid="limb-charge" className="absolute rounded-full" style={{ left: from, top: `${limb.y}%`, width: 76, height: 76, x: '-50%', y: '-50%', background: `radial-gradient(circle,#fff9d8,${color} 25%,transparent 68%)`, boxShadow: `0 0 48px ${glow}` }}
+        initial={{ scale: .08, opacity: 0 }} animate={{ scale: [.08,1.12,.42], opacity: [0,1,0] }} transition={{ duration: .68, times: [0,.62,1] }} />
+      <motion.div data-testid="limb-projectile" className="absolute rounded-[45%]" style={{ left: from, top: `${limb.y}%`, width: skillType === 'ultimate' ? 104 : 72, height: skillType === 'ultimate' ? 42 : 28, x: '-50%', y: '-50%', background: `radial-gradient(ellipse at ${side === 'player' ? '20%' : '80%'} 50%,#fffbe0,${color} 42%,transparent 76%)`, boxShadow: `0 0 28px ${glow}` }}
+        initial={{ scale: .25, opacity: 0 }} animate={{ left: [from,from,to], scale: [.25,1,1.18], opacity: [0,1,1,0], rotate: [0,direction * 8] }} transition={{ duration: .82, times: [0,.18,.82,1], ease: [.22,.72,.18,1] }} />
+      <motion.div className="absolute rounded-full" style={{ left: to, top: `${limb.y}%`, width: 165, height: 165, x: '-50%', y: '-50%', background: `radial-gradient(circle,#fffbd0 0 3%,${color}99 12%,${color}22 48%,transparent 72%)`, boxShadow: `0 0 66px ${glow}` }}
+        initial={{ scale: .08, opacity: 0 }} animate={{ scale: [.08,.08,1.28,.72], opacity: [0,0,1,0] }} transition={{ duration: 1.15, times: [0,.58,.76,1] }} />
     </>
   );
-  return (
-    <>{[0,1].map((beat) => <motion.div key={beat} className="absolute h-px" style={{ left: side === 'player' ? '20%' : '25%', top: `${43 + beat * 11}%`, width: '55%', background: `linear-gradient(90deg,transparent,${color},transparent)`, boxShadow: `0 0 18px ${glow}` }}
-      initial={{ scaleX: 0, opacity: 0 }} animate={{ scaleX: [0,1,.35], opacity: [0,1,0] }} transition={{ duration: .72, delay: .34 + beat * .34, ease: [.18,.74,.2,1] }} />)}</>
-  );
 }
 
 // ─── 1. CLAW_SLASH ────────────────────────────────────────────────────────────
@@ -1142,55 +1164,6 @@ function StrikeEffect({ strike, side, color, glow, waves, scale }: {
   }
 }
 
-// ─── Element-based effects (for skill1 / skill2 / ultimate) ──────────────────
-
-function ElementParticles({ element, side, particleScale }: {
-  element: string; side: 'player' | 'wild'; particleScale: number;
-}) {
-  const el = getEl(element);
-  const { from, to } = sides(side);
-  const count = Math.round(7 * particleScale);
-
-  const chars: Record<string, string> = {
-    Fire: '🔥', Water: '💧', Earth: '🪨', Storm: '⚡', Shadow: '✦',
-    Nature: '🍃', Electric: '⚡', Dark: '✦',
-  };
-  const char = chars[element] ?? '✦';
-
-  return (
-    <>
-      {[0, 1, 2].map((r) => (
-        <motion.div
-          key={`el-ring-${r}`}
-          className="absolute rounded-full border-2 pointer-events-none"
-          style={{
-            borderColor: el.color,
-            left: side === 'player' ? '66%' : '28%',
-            top: '44%',
-            transform: 'translate(-50%,-50%)',
-            boxShadow: `0 0 10px ${el.glow}`,
-          }}
-          initial={{ width: 16, height: 16, opacity: 0.9 }}
-          animate={{ width: [16, 200 + r * 60], height: [16, 200 + r * 60], opacity: [0.9, 0] }}
-          transition={{ duration: 0.55, delay: r * 0.09, ease: 'easeOut' }}
-        />
-      ))}
-      {Array.from({ length: count }).map((_, i) => (
-        <motion.div
-          key={`ep-${i}`}
-          className="absolute pointer-events-none"
-          style={{ left: from, top: `${38 + (i % 5) * 4}%`, fontSize: `${(13 + (i % 3) * 4) * particleScale}px` }}
-          initial={{ opacity: 0 }}
-          animate={{ left: [from, to], opacity: [0, 1, 1, 0], scale: [0.5, 1.3, 0.6], rotate: [0, (i % 2 === 0 ? 180 : -180)] }}
-          transition={{ duration: 0.52, delay: 0.08 + i * 0.05, ease: 'easeIn' }}
-        >
-          {char}
-        </motion.div>
-      ))}
-    </>
-  );
-}
-
 // ─── S-rarity extra: screen flash + power aura ───────────────────────────────
 
 function SRarityExtras({ side, color, glow }: { side: 'player' | 'wild'; color: string; glow: string }) {
@@ -1472,7 +1445,7 @@ export default function SkillCinematic({
 
         {/* ── Strike / element effect ── */}
         {effectReady && <div className="absolute inset-0" style={{ zIndex: 2 }}>
-          <PhysicalSkillStage side={attackerSide} skillType={skillType} color={el.color} glow={el.glow} />
+          {!isNormalAttack && <PhysicalSkillStage side={attackerSide} skillType={skillType} color={el.color} glow={el.glow} mythId={attackerMythId} />}
           {isNormalAttack && strike ? (
             <StrikeEffect
               strike={strike}
@@ -1482,9 +1455,7 @@ export default function SkillCinematic({
               waves={rc.hitWaves}
               scale={rc.scale}
             />
-          ) : (
-            <ElementParticles element={element} side={attackerSide} particleScale={rc.particleScale} />
-          )}
+          ) : null}
         </div>}
 
         {/* ── Impact flash ── */}
diff --git a/artifacts/monster-realms/src/components/game/BattleOverlay.tsx b/artifacts/monster-realms/src/components/game/BattleOverlay.tsx
index 6aebd2d..4b9472a 100644
--- a/artifacts/monster-realms/src/components/game/BattleOverlay.tsx
+++ b/artifacts/monster-realms/src/components/game/BattleOverlay.tsx
@@ -24,7 +24,9 @@ import type { AttackStyle } from '@/lib/myth-physics/myth-physics-engine';
 const PLAYER_PHYSICS_ID = 'battle-player-myth';
 const WILD_PHYSICS_ID = 'battle-wild-myth';
 const isPhysicsMyth = (id?: string): id is MythKind => id === 'ashquill' || id === 'flarelynx';
-const getPhysicsAttackStyle = (skillType?: string): AttackStyle => {
+const getPhysicsAttackStyle = (skillType?: string, mythId?: string): AttackStyle => {
+  if (mythId === 'ashquill' && skillType === 'skill1') return 'burst';
+  if (mythId === 'ashquill' && skillType === 'ultimate') return 'leap';
   if (skillType === 'skill1') return 'leap';
   if (skillType === 'skill2' || skillType === 'ultimate') return 'burst';
   return 'combo';
@@ -32,11 +34,10 @@ const getPhysicsAttackStyle = (skillType?: string): AttackStyle => {
 type BattleChoreography = 'melee' | 'projectile';
 type CombatActionPhase = 'idle' | 'anticipate' | 'strike' | 'recover';
 const getBattleChoreography = (skillType?: string, mythId?: string): BattleChoreography => {
-  // Flarelynx's basic is the authored twin fire-claw wave. Skill 2 / ultimates
-  // are ranged finishers; skill 1 and Ashquill's basic make physical contact.
-  if (skillType === 'skill2' || skillType === 'ultimate') return 'projectile';
-  if ((!skillType || skillType === 'normal' || skillType === 'attack') && mythId === 'flarelynx') return 'projectile';
-  return 'melee';
+  const basic = !skillType || skillType === 'normal' || skillType === 'attack';
+  if (mythId === 'flarelynx') return basic || skillType === 'skill2' || skillType === 'ultimate' ? 'projectile' : 'melee';
+  if (mythId === 'ashquill') return skillType === 'skill1' || skillType === 'skill2' ? 'projectile' : 'melee';
+  return skillType === 'skill2' || skillType === 'ultimate' ? 'projectile' : 'melee';
 };
 
 // ─── Skill types ─────────────────────────────────────────────────────────────
@@ -1244,7 +1245,7 @@ export default function BattleOverlay() {
       return;
     }
     const choreography = getBattleChoreography(cinematic.skillType, cinematic.attackerMythId);
-    const style = getPhysicsAttackStyle(cinematic.skillType);
+    const style = getPhysicsAttackStyle(cinematic.skillType, cinematic.attackerMythId);
     setCombatActionPhase('anticipate');
     const impactDelay = choreography === 'melee'
       ? (style === 'leap' ? 1120 : 980)
-- 
2.51.1

