From 8ea142416b028492bb05c7acabf2a8beb55672a7 Mon Sep 17 00:00:00 2001
From: Codex <codex@openai.com>
Date: Sat, 1 Aug 2026 18:25:04 +0100
Subject: [PATCH] Stage battle damage and polish elemental vitality

---
 .../src/components/game/BattleOverlay.tsx     | 185 +++++++++++++-----
 artifacts/monster-realms/src/index.css        |  89 ++++++---
 2 files changed, 201 insertions(+), 73 deletions(-)

diff --git a/artifacts/monster-realms/src/components/game/BattleOverlay.tsx b/artifacts/monster-realms/src/components/game/BattleOverlay.tsx
index 470d350..f4e5aff 100644
--- a/artifacts/monster-realms/src/components/game/BattleOverlay.tsx
+++ b/artifacts/monster-realms/src/components/game/BattleOverlay.tsx
@@ -715,8 +715,9 @@ function HpPlate({
   const pct = Math.max(0, Math.min(100, (currentHp / maxHp) * 100));
   const [lagPct, setLagPct] = useState(pct);
   const elColors = getElementColors(element);
-  const isFire = element.toLowerCase() === 'fire';
-  const barColor = pct > 50 ? '#22C55E' : pct > 20 ? '#EAB308' : '#EF4444';
+  const rawElementKey = element.toLowerCase();
+  const elementKey = ['fire', 'water', 'earth', 'storm', 'shadow'].includes(rawElementKey) ? rawElementKey : 'neutral';
+  const isFire = elementKey === 'fire';
   const qualLabel = QUALITY_LABEL[rarity] ?? rarity;
 
   useEffect(() => {
@@ -728,7 +729,7 @@ function HpPlate({
 
   return (
     <div
-      className={`rounded-xl px-4 py-3 battle-hp-card ${isFire ? 'battle-hp-card-fire' : ''} ${pct <= 20 ? 'battle-hp-critical' : ''}`}
+      className={`rounded-xl px-4 py-3 battle-hp-card battle-hp-card-${elementKey} ${pct <= 20 ? 'battle-hp-critical' : ''}`}
       style={{
         background: 'linear-gradient(135deg, rgba(8,8,22,0.92), rgba(4,4,14,0.96))',
         border: `2px solid ${elColors.primary}66`,
@@ -736,45 +737,44 @@ function HpPlate({
         boxShadow: `0 8px 28px rgba(0,0,0,0.82), 0 0 18px ${elColors.glow}77`,
       }}
     >
-      {/* Name + level */}
-      <div className={`flex items-baseline gap-1.5 mb-1.5 ${align === 'right' ? 'flex-row-reverse' : ''}`}>
-        <span className="font-bold text-sm text-white truncate" style={{ maxWidth: 102 }}>{name}</span>
-        <span className="text-[10px] text-white/40 font-mono shrink-0">Lv.{level}</span>
-      </div>
-      {/* Element + quality badges */}
-      <div className={`flex gap-1 mb-2 ${align === 'right' ? 'flex-row-reverse' : ''}`}>
-        <Badge
-          className="text-[9px] h-4 px-1.5"
-          style={{ background: elColors.primary + '38', color: elColors.primary, border: `1px solid ${elColors.primary}55`, boxShadow: 'none' }}
-        >
-          {element}
-        </Badge>
-        <Badge
-          className="text-[9px] h-4 px-1.5"
-          style={{ background: 'rgba(255,255,255,0.07)', color: 'rgba(255,255,255,0.55)', border: '1px solid rgba(255,255,255,0.1)', boxShadow: 'none' }}
-        >
-          {qualLabel}
-        </Badge>
+      <div className={`battle-hp-identity ${align === 'right' ? 'battle-hp-identity-right' : ''}`}>
+        <div className="battle-hp-name-group">
+          <span className="battle-hp-name">{name}</span>
+          <span className="battle-hp-level">LV.{level}</span>
+        </div>
+        <div className="battle-hp-badges">
+          <Badge
+            className="battle-hp-badge"
+            style={{ background: elColors.primary + '38', color: elColors.primary, border: `1px solid ${elColors.primary}66`, boxShadow: 'none' }}
+          >
+            {element}
+          </Badge>
+          <Badge
+            className="battle-hp-badge"
+            style={{ background: 'rgba(255,255,255,0.07)', color: 'rgba(255,255,255,0.6)', border: '1px solid rgba(255,255,255,0.13)', boxShadow: 'none' }}
+          >
+            {qualLabel}
+          </Badge>
+        </div>
       </div>
       <div className={`battle-hp-caption ${align === 'right' ? 'text-right' : ''}`}>
-        <b>HP</b><span>{Math.round(pct)}%</span>
+        <b>VITALITY</b><span>{Math.round(pct)}%</span>
       </div>
       {/* HP bar — numbers live inside the bar */}
       <div className="battle-hp-meter-wrap">
         <div
-          className={`relative w-full rounded-full overflow-hidden battle-vital-bar ${isFire ? 'battle-vital-fire' : ''}`}
+          className={`relative w-full rounded-full overflow-hidden battle-vital-bar battle-vital-${elementKey}`}
           style={{ background: 'rgba(0,0,0,0.72)', border: '1px solid rgba(255,255,255,0.16)' }}
         >
           <div className="absolute inset-y-0 left-0 rounded-full battle-hp-lag" style={{ width: `${lagPct}%` }} />
           <div
-            className={`absolute inset-y-0 left-0 rounded-full hp-bar-fill ${isFire ? 'battle-fire-hp-fill' : ''}`}
+            className={`absolute inset-y-0 left-0 rounded-full hp-bar-fill battle-element-hp-fill battle-${elementKey}-hp-fill`}
             style={{
               width: `${pct}%`,
-              background: isFire ? undefined : `linear-gradient(90deg, ${barColor}88, ${barColor}dd)`,
-              boxShadow: isFire ? undefined : `0 0 6px ${barColor}66`,
-            }}
+              '--hp-element-color': elColors.primary,
+            } as React.CSSProperties}
           >
-            {isFire && <><i /><i /><i /><i /><i /></>}
+            <i /><i /><i /><i /><i />
           </div>
           {/* HP text overlay */}
           <div className="absolute inset-0 flex items-center justify-center pointer-events-none">
@@ -1112,6 +1112,13 @@ export default function BattleOverlay() {
     attackerMythId?: string; attackerRarity?: string; skillType?: string;
   } | null>(null);
 
+  // The API resolves the player's action and the wild counterattack together.
+  // Keep visual HP separate so each defender loses health at its own impact.
+  const [displayedWildHp, setDisplayedWildHp] = useState<number | null>(null);
+  const [displayedPlayerHp, setDisplayedPlayerHp] = useState<number | null>(null);
+  const playerImpactApplied = useRef(false);
+  const wildImpactApplied = useRef(false);
+
   const prevWildHp      = useRef<number | null>(null);
   const prevPlayerHp    = useRef<number | null>(null);
   const prevCapturedId  = useRef<string | null>(null);
@@ -1153,7 +1160,16 @@ export default function BattleOverlay() {
     const wd = battleData.wildMonster;
     const pd = battleData.playerMonster;
 
-    if (prevWildHp.current !== null && wd.currentHp < prevWildHp.current) {
+    const actionSequenceActive = cinematic !== null || opponentTurnActive || pendingBattleResult.current !== null;
+
+    // Polling may receive the final HP for both turns while the first animation
+    // is still playing. Never let that authoritative snapshot bypass staging.
+    if (!actionSequenceActive) {
+      setDisplayedWildHp(wd.currentHp);
+      setDisplayedPlayerHp(pd.currentHp);
+    }
+
+    if (!actionSequenceActive && prevWildHp.current !== null && wd.currentHp < prevWildHp.current) {
       if (optimisticWildHitRef.current > 0) optimisticWildHitRef.current -= 1;
       else setWildShake((k) => k + 1);
       // Floating damage number on the wild myth
@@ -1177,7 +1193,7 @@ export default function BattleOverlay() {
         });
       }
     }
-    if (prevPlayerHp.current !== null && pd.currentHp < prevPlayerHp.current) {
+    if (!actionSequenceActive && prevPlayerHp.current !== null && pd.currentHp < prevPlayerHp.current) {
       if (optimisticPlayerHitRef.current > 0) optimisticPlayerHitRef.current -= 1;
       else setPlayerShake((k) => k + 1);
       // Floating damage number on the player myth
@@ -1219,7 +1235,7 @@ export default function BattleOverlay() {
         setTimeout(() => { endBattle(); setCaptureMsg(null); }, 4000);
       }
     }
-  }, [battleData]);
+  }, [battleData, cinematic, opponentTurnActive, battle.active, updateBattle, endBattle]);
 
   // Trigger entrances + PvP intro cinematic when a fresh battle starts
   useEffect(() => {
@@ -1230,6 +1246,10 @@ export default function BattleOverlay() {
       setShowBattleIntro(true);
       setShowPlayerEntrance(true);
       setShowWildEntrance(true);
+      setDisplayedWildHp(battleData?.wildMonster.currentHp ?? null);
+      setDisplayedPlayerHp(battleData?.playerMonster.currentHp ?? null);
+      playerImpactApplied.current = false;
+      wildImpactApplied.current = false;
       const t = setTimeout(() => setShowBattleIntro(false), 2800);
       return () => clearTimeout(t);
     }
@@ -1257,6 +1277,65 @@ export default function BattleOverlay() {
     }
   }, [isOver]);
 
+  const applyStagedImpact = useCallback((defender: 'wild' | 'player') => {
+    const result = pendingBattleResult.current;
+    if (!result) return false;
+
+    if (defender === 'wild') {
+      if (playerImpactApplied.current) return true;
+      playerImpactApplied.current = true;
+      const nextHp = result.wildMonster.currentHp;
+      setDisplayedWildHp(previous => {
+        const fromHp = previous ?? result.wildMonster.maxHp;
+        const damage = Math.max(0, fromHp - nextHp);
+        if (damage > 0) {
+          const id = ++floatIdRef.current;
+          setDamageFloats(items => [...items, { id, dmg: damage, side: 'wild', crit: !!cinematic?.isCritical }]);
+          window.setTimeout(() => setDamageFloats(items => items.filter(item => item.id !== id)), 1600);
+        }
+        return nextHp;
+      });
+      setWildShake(value => value + 1);
+      setHitFlashWild(value => value + 1);
+      setArenaShakeCrit(!!cinematic?.isCritical);
+      setArenaShakeId(value => value + 1);
+      if (nextHp === 0) {
+        setFaintCinematic({
+          side: 'wild', mythId: result.wildMonster.species?.id ?? '',
+          element: result.wildMonster.species?.element ?? 'Fire',
+          rarity: result.wildMonster.species?.rarity ?? 'C',
+        });
+      }
+      return true;
+    }
+
+    if (wildImpactApplied.current) return true;
+    wildImpactApplied.current = true;
+    const nextHp = result.playerMonster.currentHp;
+    setDisplayedPlayerHp(previous => {
+      const fromHp = previous ?? result.playerMonster.maxHp;
+      const damage = Math.max(0, fromHp - nextHp);
+      if (damage > 0) {
+        const id = ++floatIdRef.current;
+        setDamageFloats(items => [...items, { id, dmg: damage, side: 'player', crit: !!cinematic?.isCritical }]);
+        window.setTimeout(() => setDamageFloats(items => items.filter(item => item.id !== id)), 1600);
+      }
+      return nextHp;
+    });
+    setPlayerShake(value => value + 1);
+    setHitFlashPlayer(value => value + 1);
+    setArenaShakeCrit(!!cinematic?.isCritical);
+    setArenaShakeId(value => value + 1);
+    if (nextHp === 0) {
+      setFaintCinematic({
+        side: 'player', mythId: result.playerMonster.species?.id ?? '',
+        element: result.playerMonster.species?.element ?? 'Fire',
+        rarity: result.playerMonster.species?.rarity ?? 'C',
+      });
+    }
+    return true;
+  }, [cinematic?.isCritical]);
+
   // ── Lunge: increment the lunge key for the attacker on each new cinematic ─
   useEffect(() => {
     if (!cinematic) return;
@@ -1275,21 +1354,17 @@ export default function BattleOverlay() {
     if (cinematic.attackerSide === 'player') {
       setPlayerLungeKey(k => k + 1);
       impactTimer = window.setTimeout(() => {
-        optimisticWildHitRef.current += 1;
-        setWildShake(k => k + 1);
-        window.setTimeout(() => { if (optimisticWildHitRef.current > 0) optimisticWildHitRef.current -= 1; }, 4000);
+        applyStagedImpact('wild');
       }, impactDelay);
     } else {
       setWildLungeKey(k => k + 1);
       impactTimer = window.setTimeout(() => {
-        optimisticPlayerHitRef.current += 1;
-        setPlayerShake(k => k + 1);
-        window.setTimeout(() => { if (optimisticPlayerHitRef.current > 0) optimisticPlayerHitRef.current -= 1; }, 4000);
+        applyStagedImpact('player');
       }, impactDelay);
     }
     return () => window.clearTimeout(impactTimer);
   // eslint-disable-next-line react-hooks/exhaustive-deps
-  }, [cinematic, mythPhysics]);
+  }, [cinematic, mythPhysics, applyStagedImpact]);
 
   // ── Round tracking — banner fires AFTER opponent's turn ends ────────────
   // When the API returns a new round number we don't immediately show the banner
@@ -1411,24 +1486,31 @@ export default function BattleOverlay() {
     const wild = pendingWildCinematic.current;
 
     if (cinematic?.phase === 'player') {
-      // Apply result, then maybe show wild cinematic
+      // Fallback for unusually slow responses: if the contact timer ran before
+      // the API resolved, stage the defender's HP at cinematic completion.
+      applyStagedImpact('wild');
+      // Apply authoritative battle metadata, while displayed HP remains staged.
       if (result) {
         updateBattle(result);
         if (battle.battleId) {
           queryClient.invalidateQueries({ queryKey: getGetBattleQueryKey(battle.battleId) });
         }
-        pendingBattleResult.current = null;
       }
-      if (wild && result?.status === 'active') {
+      // A lethal counterattack marks the server battle as finished, but its
+      // animation and player HP loss still have to play before the result UI.
+      if (wild) {
         // Hand off to opponent turn phase — the countdown will fire the wild cinematic
         // (pendingWildCinematic.current still holds `wild`; opponent effect consumes it)
         setOpponentTurnActive(true);
       } else {
         pendingWildCinematic.current = null;
+        pendingBattleResult.current = null;
         setCinematic(null);
       }
     } else {
-      // Wild cinematic done — clear, then announce new round if pending
+      // The counterattack is a separate visual event: only now may player HP fall.
+      applyStagedImpact('player');
+      pendingBattleResult.current = null;
       setCinematic(null);
       if (pendingRoundRef.current > 0) {
         const nextRound = pendingRoundRef.current;
@@ -1438,12 +1520,15 @@ export default function BattleOverlay() {
         setTimeout(() => setShowRoundBanner(false), 2500);
       }
     }
-  }, [cinematic, battle.battleId, updateBattle, queryClient]);
+  }, [cinematic, battle.battleId, updateBattle, queryClient, applyStagedImpact]);
 
   // ── Skill / attack action ─────────────────────────────────────────────────
   const handleSkillAction = useCallback(async (action: ActionType) => {
     if (!battle.battleId || isPending || isOver || cinematic) return;
 
+    playerImpactApplied.current = false;
+    wildImpactApplied.current = false;
+
     const playerSkills = getDisplaySkills(playerMonster?.species ?? {});
     const skill = getSkillForAction(action, playerSkills);
     const skillElement = skill.element ?? playerMonster?.species?.element ?? 'Fire';
@@ -1606,7 +1691,9 @@ export default function BattleOverlay() {
   const logText   = lastLog?.description ?? (isOver ? getEndText(status) : 'What will you do?');
   const logActor  = lastLog?.actor === 'player' ? 'player' : lastLog?.actor === 'wild' ? 'wild' : 'system';
 
-  const wildHpPct   = (wildMonster.currentHp / wildMonster.maxHp) * 100;
+  const visibleWildHp = displayedWildHp ?? wildMonster.currentHp;
+  const visiblePlayerHp = displayedPlayerHp ?? playerMonster.currentHp;
+  const wildHpPct   = (visibleWildHp / wildMonster.maxHp) * 100;
   const wildColors  = getElementColors(wildMonster.species.element);
 
   return (
@@ -1747,7 +1834,7 @@ export default function BattleOverlay() {
               level={wildMonster.level}
               element={wildMonster.species.element}
               rarity={wildMonster.species.rarity}
-              currentHp={wildMonster.currentHp}
+              currentHp={visibleWildHp}
               maxHp={wildMonster.maxHp}
               align="left"
               statusEffect={wildMonster.statusEffect}
@@ -1815,7 +1902,7 @@ export default function BattleOverlay() {
               level={playerMonster.level}
               element={playerMonster.species.element}
               rarity={playerMonster.species.rarity}
-              currentHp={playerMonster.currentHp}
+              currentHp={visiblePlayerHp}
               maxHp={playerMonster.maxHp}
               align="right"
               statusEffect={playerMonster.statusEffect}
@@ -1898,7 +1985,7 @@ export default function BattleOverlay() {
             key={f.id}
             className={`absolute ${f.crit ? 'dmg-float-crit' : 'dmg-float'}`}
             style={{
-              ...(f.side === 'wild' ? { left: '12%' } : { right: '12%' }),
+              ...(f.side === 'wild' ? { left: '30.9%', transform: 'translateX(-50%)' } : { left: '69.1%', transform: 'translateX(-50%)' }),
               bottom: '54%',
               zIndex: 12,
               fontSize: f.crit ? 34 : 22,
@@ -2195,7 +2282,7 @@ export default function BattleOverlay() {
             playerMythName={playerMonster.species.name}
             playerMythElement={playerMonster.species.element}
             playerMythRarity={playerMonster.species.rarity}
-            playerHpPct={(playerMonster.currentHp / playerMonster.maxHp) * 100}
+            playerHpPct={(visiblePlayerHp / playerMonster.maxHp) * 100}
             totalRounds={battle.battle.round ?? 1}
             battleLog={log as { actor: string; damageDealt: number | null; critical: boolean; description?: string }[]}
           />
diff --git a/artifacts/monster-realms/src/index.css b/artifacts/monster-realms/src/index.css
index 2d29141..eed4edf 100644
--- a/artifacts/monster-realms/src/index.css
+++ b/artifacts/monster-realms/src/index.css
@@ -508,13 +508,17 @@
   .battle-idle-bob { animation: battle-idle-bob 2s ease-in-out infinite; }
 
   .battle-combatant-wild {
-    left: clamp(3%, 7vw, 9%);
-    bottom: 2%;
+    left: 30.9%;
+    right: auto;
+    bottom: 3.2%;
+    translate: -50% 0;
   }
 
   .battle-combatant-player {
-    right: clamp(3%, 7vw, 9%);
-    bottom: 2%;
+    left: 69.1%;
+    right: auto;
+    bottom: 3.2%;
+    translate: -50% 0;
   }
 
   .battle-screen-shell { height: 100dvh; max-height: 100dvh; overflow: hidden; isolation: isolate; }
@@ -592,13 +596,16 @@
   @keyframes battle-ash-drift { 0%{opacity:0;translate:0 22px} 18%{opacity:.7} 100%{opacity:0;translate:38px -150px} }
 
   .battle-hit-wild {
-    left: clamp(3%, 8vw, 10%);
+    left: 30.9%;
     bottom: 7%;
+    translate: -50% 0;
   }
 
   .battle-hit-player {
-    right: clamp(3%, 8vw, 10%);
+    left: 69.1%;
+    right: auto;
     bottom: 7%;
+    translate: -50% 0;
   }
 
   .battle-ground-perspective {
@@ -627,8 +634,8 @@
     filter: blur(2px);
   }
 
-  .battle-stage-pool-wild { left: clamp(0%, 4vw, 6%); }
-  .battle-stage-pool-player { right: clamp(0%, 4vw, 6%); }
+  .battle-stage-pool-wild { left:30.9%; transform:translateX(-50%); }
+  .battle-stage-pool-player { left:69.1%; right:auto; transform:translateX(-50%); }
 
   .battle-skill-grid {
     display: grid;
@@ -644,8 +651,8 @@
 
   @media (max-width: 700px) {
     .battle-trainer { left: 76% !important; bottom: 48% !important; transform: translateX(-50%) scale(.76) !important; }
-    .battle-combatant-wild { left: 1%; }
-    .battle-combatant-player { right: 1%; }
+    .battle-combatant-wild { left: 28%; right:auto; }
+    .battle-combatant-player { left: 72%; right:auto; }
   }
 
   .battle-arena-vignette {
@@ -678,18 +685,35 @@
   .battle-hp-anchor { transform: scale(1); z-index: 15; }
   .battle-hp-anchor-left { transform-origin: top left; }
   .battle-hp-anchor-right { transform-origin: top right; }
-  .battle-hp-over-myth { position:relative; z-index:16; margin-bottom:10px; pointer-events:none; }
-  .battle-hp-over-myth-wild { align-self:flex-start; }
-  .battle-hp-over-myth-player { align-self:flex-end; }
-  .battle-hp-card { position:relative; overflow:visible; width:clamp(226px,23vw,290px); }
+  .battle-hp-over-myth { position:relative; z-index:16; margin-bottom:14px; pointer-events:none; }
+  .battle-hp-over-myth-wild,.battle-hp-over-myth-player { align-self:center; }
+  .battle-hp-card { position:relative; overflow:visible; width:clamp(230px,20vw,286px); padding:10px 12px !important; border-radius:14px !important; }
   .battle-hp-card::before { content:""; position:absolute; inset:0; pointer-events:none; background:linear-gradient(115deg,rgba(255,255,255,.09),transparent 28% 72%,rgba(255,255,255,.035)); }
   .battle-hp-card::after { content:""; position:absolute; left:10%; right:10%; top:0; height:1px; background:linear-gradient(90deg,transparent,rgba(255,255,255,.5),transparent); opacity:.55; }
+  .battle-hp-identity { display:flex; align-items:center; justify-content:space-between; gap:8px; margin-bottom:7px; }
+  .battle-hp-identity-right { flex-direction:row-reverse; }
+  .battle-hp-name-group { min-width:0; display:flex; align-items:baseline; gap:6px; }
+  .battle-hp-identity-right .battle-hp-name-group { flex-direction:row-reverse; }
+  .battle-hp-name { overflow:hidden; text-overflow:ellipsis; color:#fff; font:900 13px var(--font-mono,monospace); letter-spacing:.025em; text-shadow:0 2px 8px #000; white-space:nowrap; }
+  .battle-hp-level { flex:none; color:rgba(255,255,255,.52); font:800 8px var(--font-mono,monospace); letter-spacing:.08em; }
+  .battle-hp-badges { flex:none; display:flex; gap:3px; }
+  .battle-hp-identity-right .battle-hp-badges { flex-direction:row-reverse; }
+  .battle-hp-badge { height:16px !important; padding:0 5px !important; font:800 7px var(--font-mono,monospace) !important; letter-spacing:.04em; }
   .battle-hp-caption { display:flex; align-items:center; justify-content:space-between; margin:0 2px 4px; color:rgba(255,255,255,.72); font:800 9px var(--font-mono,monospace); letter-spacing:.13em; }
-  .battle-hp-caption b { color:#fff; font-size:11px; text-shadow:0 0 8px rgba(255,255,255,.25); }
-  .battle-vital-bar { height:28px; box-shadow:inset 0 2px 7px rgba(0,0,0,.8),0 1px 0 rgba(255,255,255,.1),0 0 14px rgba(34,197,94,.1); }
+  .battle-hp-caption b { color:#fff; font-size:9px; letter-spacing:.18em; text-shadow:0 0 8px rgba(255,255,255,.25); }
+  .battle-vital-bar { height:30px; box-shadow:inset 0 2px 7px rgba(0,0,0,.8),0 1px 0 rgba(255,255,255,.1),0 0 14px rgba(34,197,94,.1); }
   .battle-hp-meter-wrap { position:relative; isolation:isolate; }
   .battle-hp-card-fire { border-color:rgba(251,92,36,.72) !important; box-shadow:0 8px 28px rgba(0,0,0,.82),0 0 18px rgba(249,74,20,.42) !important; }
   .battle-vital-fire { background:linear-gradient(180deg,#211008,#070201) !important; box-shadow:inset 0 2px 8px #000,0 0 14px rgba(255,83,15,.34); }
+  .battle-hp-card-water { box-shadow:0 8px 28px rgba(0,0,0,.82),0 0 18px rgba(37,160,255,.32) !important; }
+  .battle-hp-card-earth { box-shadow:0 8px 28px rgba(0,0,0,.82),0 0 18px rgba(176,126,62,.28) !important; }
+  .battle-hp-card-storm { box-shadow:0 8px 28px rgba(0,0,0,.82),0 0 18px rgba(122,216,255,.38) !important; }
+  .battle-hp-card-shadow { box-shadow:0 8px 28px rgba(0,0,0,.82),0 0 20px rgba(162,78,255,.38) !important; }
+  .battle-vital-water { background:linear-gradient(180deg,#061d31,#020810) !important; box-shadow:inset 0 2px 8px #000,0 0 14px rgba(26,145,255,.28); }
+  .battle-vital-earth { background:linear-gradient(180deg,#241a0f,#090603) !important; box-shadow:inset 0 2px 8px #000,0 0 12px rgba(191,135,60,.22); }
+  .battle-vital-storm { background:linear-gradient(180deg,#102536,#03080e) !important; box-shadow:inset 0 2px 8px #000,0 0 16px rgba(80,205,255,.34); }
+  .battle-vital-shadow { background:linear-gradient(180deg,#1c0b2b,#06020a) !important; box-shadow:inset 0 2px 8px #000,0 0 16px rgba(153,70,255,.32); }
+  .battle-element-hp-fill { overflow:visible; transform-origin:left center; transition:width .48s cubic-bezier(.2,.75,.25,1); }
   .battle-fire-hp-fill {
     overflow:visible; transform-origin:left center;
     background:linear-gradient(180deg,#fff2a4 0 8%,#ffbf32 22%,#ff5b18 58%,#8e1609 100%);
@@ -713,6 +737,23 @@
   @keyframes battle-fire-hp-flame { from{transform:translateY(2px) scaleY(.88);filter:saturate(1)} to{transform:translateY(-1px) scaleY(1.08);filter:saturate(1.2) brightness(1.08)} }
   @keyframes battle-fire-hp-ember { 0%{opacity:0;transform:translate(0,3px) scale(.5)} 30%{opacity:1} 100%{opacity:0;transform:translate(5px,-18px) scale(.1)} }
   @keyframes battle-fire-ash-fall { 0%{opacity:0;transform:translate(0,-4px) rotate(0)} 18%{opacity:.82} 100%{opacity:0;transform:translate(9px,34px) rotate(190deg)} }
+  .battle-water-hp-fill { background:linear-gradient(180deg,#baf3ff 0 8%,#35bdf4 22%,#087bd0 62%,#064480 100%); box-shadow:0 0 7px #73ddff,0 0 15px rgba(24,145,255,.64),inset 0 -6px 9px rgba(2,45,100,.65); }
+  .battle-water-hp-fill::before { content:"";position:absolute;inset:0;background:radial-gradient(ellipse at 50% -10%,rgba(255,255,255,.9) 0 6%,transparent 8%),repeating-radial-gradient(ellipse at 50% 120%,transparent 0 13px,rgba(183,240,255,.42) 14px 16px,transparent 17px 25px);background-size:38px 28px,54px 30px;animation:battle-water-flow 1.35s linear infinite; }
+  .battle-water-hp-fill > i { position:absolute;z-index:2;bottom:4px;width:4px;height:4px;border:1px solid #d7f7ff;border-radius:50%;opacity:0;animation:battle-water-bubble 1.55s ease-out infinite; }
+  .battle-earth-hp-fill { background:repeating-linear-gradient(112deg,#e0ae63 0 13px,#9e632d 14px 25px,#593418 26px 29px); box-shadow:0 0 7px #d69b4e,0 0 13px rgba(154,96,39,.54),inset 0 -7px 9px rgba(55,28,10,.72); }
+  .battle-earth-hp-fill::before { content:"";position:absolute;inset:2px 0;background:repeating-linear-gradient(28deg,transparent 0 18px,rgba(255,225,157,.28) 19px 20px,transparent 21px 34px); }
+  .battle-earth-hp-fill > i { position:absolute;z-index:2;bottom:5px;width:4px;height:5px;background:#ffe0a0;clip-path:polygon(50% 0,100% 100%,0 78%);opacity:.7;animation:battle-earth-glint 1.8s ease-in-out infinite; }
+  .battle-storm-hp-fill { background:linear-gradient(180deg,#e8fbff 0 7%,#54dcff 24%,#3576e8 58%,#5530ac 100%);box-shadow:0 0 8px #9cecff,0 0 17px rgba(72,174,255,.8),inset 0 -6px 9px rgba(48,27,130,.62); }
+  .battle-storm-hp-fill::before { content:"";position:absolute;inset:1px 0;background:repeating-linear-gradient(128deg,transparent 0 17px,rgba(255,255,255,.92) 18px 19px,transparent 20px 34px);animation:battle-storm-current .55s linear infinite; }
+  .battle-storm-hp-fill > i { position:absolute;z-index:2;bottom:3px;width:3px;height:3px;border-radius:50%;background:white;box-shadow:0 0 6px #8eeaff;animation:battle-storm-spark .8s ease-out infinite; }
+  .battle-shadow-hp-fill { background:linear-gradient(180deg,#e2b6ff 0 5%,#9b48e8 25%,#53168f 62%,#210633 100%);box-shadow:0 0 8px #c778ff,0 0 18px rgba(124,39,210,.76),inset 0 -7px 10px rgba(24,2,38,.72); }
+  .battle-shadow-hp-fill::before { content:"";position:absolute;inset:-20% 0;background:radial-gradient(ellipse at 20% 50%,rgba(255,255,255,.28),transparent 28%),radial-gradient(ellipse at 70% 45%,rgba(28,0,52,.78),transparent 34%);background-size:70px 32px;animation:battle-shadow-drift 1.7s ease-in-out infinite alternate; }
+  .battle-shadow-hp-fill > i { position:absolute;z-index:2;bottom:3px;width:4px;height:4px;border-radius:50%;background:#e6b8ff;box-shadow:0 0 7px #a74cff;animation:battle-shadow-mote 1.45s ease-out infinite; }
+  .battle-neutral-hp-fill { background:linear-gradient(90deg,var(--hp-element-color,#22c55e),color-mix(in srgb,var(--hp-element-color,#22c55e) 65%,white));box-shadow:0 0 9px var(--hp-element-color,#22c55e); }
+  .battle-water-hp-fill > i:nth-child(1),.battle-earth-hp-fill > i:nth-child(1),.battle-storm-hp-fill > i:nth-child(1),.battle-shadow-hp-fill > i:nth-child(1){left:10%;animation-delay:-.2s}.battle-water-hp-fill > i:nth-child(2),.battle-earth-hp-fill > i:nth-child(2),.battle-storm-hp-fill > i:nth-child(2),.battle-shadow-hp-fill > i:nth-child(2){left:29%;animation-delay:-.8s}.battle-water-hp-fill > i:nth-child(3),.battle-earth-hp-fill > i:nth-child(3),.battle-storm-hp-fill > i:nth-child(3),.battle-shadow-hp-fill > i:nth-child(3){left:49%;animation-delay:-1.2s}.battle-water-hp-fill > i:nth-child(4),.battle-earth-hp-fill > i:nth-child(4),.battle-storm-hp-fill > i:nth-child(4),.battle-shadow-hp-fill > i:nth-child(4){left:70%;animation-delay:-.5s}.battle-water-hp-fill > i:nth-child(5),.battle-earth-hp-fill > i:nth-child(5),.battle-storm-hp-fill > i:nth-child(5),.battle-shadow-hp-fill > i:nth-child(5){left:90%;animation-delay:-1s}
+  @keyframes battle-water-flow{to{background-position:38px 0,54px 0}} @keyframes battle-water-bubble{0%{opacity:0;transform:translateY(3px) scale(.5)}25%{opacity:.8}100%{opacity:0;transform:translate(4px,-19px) scale(1.3)}}
+  @keyframes battle-earth-glint{0%,70%,100%{opacity:.25;filter:brightness(.8)}82%{opacity:1;filter:brightness(1.8)}} @keyframes battle-storm-current{to{background-position:34px 0}} @keyframes battle-storm-spark{0%{opacity:0;transform:translate(0,2px)}30%{opacity:1}100%{opacity:0;transform:translate(7px,-14px)}}
+  @keyframes battle-shadow-drift{to{background-position:45px 0,-36px 0;filter:brightness(1.2)}} @keyframes battle-shadow-mote{0%{opacity:0;transform:translateY(2px) scale(.4)}30%{opacity:.8}100%{opacity:0;transform:translate(-5px,-17px) scale(1.4)}}
   .battle-hp-lag { background:linear-gradient(90deg,#f4d86a,#fff0a2); opacity:.8; transition:width .72s cubic-bezier(.18,.72,.2,1); box-shadow:0 0 9px #ffe78388; }
   .battle-hp-critical { animation:battle-hp-critical 1.15s ease-in-out infinite alternate; }
   @keyframes battle-hp-critical { to { border-color:#ef444488; box-shadow:0 4px 24px rgba(0,0,0,.78),0 0 18px rgba(239,68,68,.3); } }
@@ -1050,11 +1091,11 @@
   .battle-turn-camera-focus {
     position: absolute; inset: 0; z-index: 10; pointer-events: none;
     transition: background 1.1s ease, opacity .8s ease;
-    background: radial-gradient(ellipse 34% 54% at 78% 62%, rgba(255,255,255,.055), transparent 68%);
+    background: radial-gradient(ellipse 34% 54% at 69.1% 61.8%, rgba(255,255,255,.055), transparent 68%);
     mix-blend-mode: screen;
   }
   .battle-focus-wild .battle-turn-camera-focus {
-    background: radial-gradient(ellipse 34% 54% at 22% 62%, rgba(255,255,255,.055), transparent 68%);
+    background: radial-gradient(ellipse 34% 54% at 30.9% 61.8%, rgba(255,255,255,.055), transparent 68%);
   }
   .battle-combatant-player, .battle-combatant-wild {
     transition: transform 1.05s cubic-bezier(.2,.72,.24,1), filter .8s ease, opacity .8s ease;
@@ -1087,12 +1128,12 @@
       bottom: 1%;
       transform-origin: bottom center;
     }
-    .battle-combatant-wild { left: -1%; }
-    .battle-combatant-player { right: -1%; }
+    .battle-combatant-wild { left:28%; right:auto; }
+    .battle-combatant-player { left:72%; right:auto; }
     .myth-lunge-left { --battle-lunge-x: -47vw; --battle-lunge-recoil: -45vw; }
     .myth-lunge-right { --battle-lunge-x: 47vw; --battle-lunge-recoil: 45vw; }
-    .battle-hit-wild { left: 11%; }
-    .battle-hit-player { right: 11%; }
+    .battle-hit-wild { left:28%; }
+    .battle-hit-player { left:72%; right:auto; }
     .battle-hp-anchor { scale:1; }
     .battle-hp-over-myth { transform:scale(1.34); transform-origin:bottom center; margin-bottom:18px; }
     .battle-hp-card { width:min(39vw,190px); padding:8px 9px !important; }
@@ -1103,8 +1144,8 @@
   @media (max-width: 480px) {
     .battle-combatant-wild,
     .battle-combatant-player { scale: .52; bottom: 2%; }
-    .battle-combatant-wild { left: -5%; }
-    .battle-combatant-player { right: -5%; }
+    .battle-combatant-wild { left:27%; right:auto; }
+    .battle-combatant-player { left:73%; right:auto; }
     .battle-hp-over-myth { transform:scale(1.62); margin-bottom:25px; }
     .battle-hp-card { width:37vw; }
     .myth-lunge-left { --battle-lunge-x: -32vw; --battle-lunge-recoil: -30vw; }
-- 
2.51.1

