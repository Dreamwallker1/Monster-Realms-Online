From 02695e98ed7e6f7c7b7f3f2d100c4e38a42fd42c Mon Sep 17 00:00:00 2001
From: Codex <codex@openai.com>
Date: Sat, 1 Aug 2026 13:20:40 +0300
Subject: [PATCH] Stage readable contact and projectile combat

---
 .../src/components/battle/SkillCinematic.tsx  |  6 +-
 .../src/components/game/BattleOverlay.tsx     | 20 ++++--
 artifacts/monster-realms/src/index.css        | 62 ++++++++++++++-----
 3 files changed, 64 insertions(+), 24 deletions(-)

diff --git a/artifacts/monster-realms/src/components/battle/SkillCinematic.tsx b/artifacts/monster-realms/src/components/battle/SkillCinematic.tsx
index 521d1cd..e992b0d 100644
--- a/artifacts/monster-realms/src/components/battle/SkillCinematic.tsx
+++ b/artifacts/monster-realms/src/components/battle/SkillCinematic.tsx
@@ -1391,11 +1391,11 @@ export default function SkillCinematic({
         <motion.div
           className="absolute inset-0"
           style={{
-            background: isFlarelynxBasic ? 'rgba(82, 18, 3, 0.12)' : el.bg,
-            backdropFilter: isFlarelynxBasic ? 'brightness(0.92)' : 'brightness(0.65)',
+            background: isFlarelynxBasic ? 'rgba(255, 91, 24, 0.06)' : el.bg,
+            backdropFilter: 'brightness(1.04) saturate(1.06)',
           }}
           initial={{ opacity: 0 }}
-          animate={{ opacity: [0, 1, 1, 0] }}
+          animate={{ opacity: [0, isFlarelynxBasic ? 0.36 : 0.22, isFlarelynxBasic ? 0.24 : 0.14, 0] }}
           transition={{ duration: timeout / 1000, times: [0, 0.08, 0.82, 1] }}
         />
 
diff --git a/artifacts/monster-realms/src/components/game/BattleOverlay.tsx b/artifacts/monster-realms/src/components/game/BattleOverlay.tsx
index 60e1e9c..08b610b 100644
--- a/artifacts/monster-realms/src/components/game/BattleOverlay.tsx
+++ b/artifacts/monster-realms/src/components/game/BattleOverlay.tsx
@@ -29,6 +29,14 @@ const getPhysicsAttackStyle = (skillType?: string): AttackStyle => {
   if (skillType === 'skill2' || skillType === 'ultimate') return 'burst';
   return 'combo';
 };
+type BattleChoreography = 'melee' | 'projectile';
+const getBattleChoreography = (skillType?: string, mythId?: string): BattleChoreography => {
+  // Flarelynx's basic is the authored twin fire-claw wave. Skill 2 / ultimates
+  // are ranged finishers; skill 1 and Ashquill's basic make physical contact.
+  if (skillType === 'skill2' || skillType === 'ultimate') return 'projectile';
+  if ((!skillType || skillType === 'normal' || skillType === 'attack') && mythId === 'flarelynx') return 'projectile';
+  return 'melee';
+};
 
 // ─── Skill types ─────────────────────────────────────────────────────────────
 
@@ -1228,6 +1236,7 @@ export default function BattleOverlay() {
   // ── Lunge: increment the lunge key for the attacker on each new cinematic ─
   useEffect(() => {
     if (!cinematic) return;
+    const choreography = getBattleChoreography(cinematic.skillType, cinematic.attackerMythId);
     mythPhysics.attack(
       cinematic.attackerSide === 'player' ? PLAYER_PHYSICS_ID : WILD_PHYSICS_ID,
       cinematic.attackerSide === 'player' ? WILD_PHYSICS_ID : PLAYER_PHYSICS_ID,
@@ -1236,8 +1245,9 @@ export default function BattleOverlay() {
       getPhysicsAttackStyle(cinematic.skillType),
     );
     let impactTimer: number;
-    const impactDelay = getPhysicsAttackStyle(cinematic.skillType) === 'combo' ? 780
-      : getPhysicsAttackStyle(cinematic.skillType) === 'leap' ? 940 : 790;
+    const impactDelay = choreography === 'melee'
+      ? (getPhysicsAttackStyle(cinematic.skillType) === 'leap' ? 1120 : 980)
+      : (getPhysicsAttackStyle(cinematic.skillType) === 'burst' ? 1180 : 820);
     if (cinematic.attackerSide === 'player') {
       setPlayerLungeKey(k => k + 1);
       impactTimer = window.setTimeout(() => {
@@ -1579,7 +1589,7 @@ export default function BattleOverlay() {
     <div className="fixed inset-0 z-50 flex flex-col battle-screen-in battle-screen-shell" style={{ fontFamily: 'var(--font-mono, monospace)' }}>
 
       {/* ── ARENA ─────────────────────────────────────────────────────────── */}
-      <div className={`relative flex-1 min-h-0 overflow-hidden battle-arena ${opponentTurnActive || cinematic?.attackerSide === 'wild' ? 'battle-focus-wild' : 'battle-focus-player'} ${cinematic ? 'battle-focus-cinematic' : ''}`}>
+      <div className={`relative flex-1 min-h-0 overflow-hidden battle-arena ${opponentTurnActive || cinematic?.attackerSide === 'wild' ? 'battle-focus-wild' : 'battle-focus-player'} ${cinematic ? `battle-focus-cinematic battle-action-${getBattleChoreography(cinematic.skillType, cinematic.attackerMythId)}` : ''}`}>
 
         {/* Sky */}
         <div className="absolute inset-0" style={{ background: theme.skyGrad }} />
@@ -1748,7 +1758,7 @@ export default function BattleOverlay() {
           {/* Inner lunge wrapper — re-keyed on each wild attack to replay animation */}
           <div
             key={`wlunge-${wildLungeKey}`}
-            className={`flex flex-col items-center ${wildLungeKey > 0 ? 'myth-lunge-right' : ''}`}
+            className={`flex flex-col items-center ${wildLungeKey > 0 && cinematic?.attackerSide === 'wild' ? (getBattleChoreography(cinematic.skillType, cinematic.attackerMythId) === 'projectile' ? 'myth-cast-right' : 'myth-lunge-right') : ''}`}
           >
             {/* Type matchup badge */}
             {(() => {
@@ -1794,7 +1804,7 @@ export default function BattleOverlay() {
           {/* Inner lunge wrapper — re-keyed on each player attack to replay animation */}
           <div
             key={`plunge-${playerLungeKey}`}
-            className={`flex flex-col items-center ${playerLungeKey > 0 ? 'myth-lunge-left' : ''}`}
+            className={`flex flex-col items-center ${playerLungeKey > 0 && cinematic?.attackerSide === 'player' ? (getBattleChoreography(cinematic.skillType, cinematic.attackerMythId) === 'projectile' ? 'myth-cast-left' : 'myth-lunge-left') : ''}`}
           >
             <div className="text-[10px] font-bold tracking-widest uppercase mb-2 text-center text-white/50">
               Your Myth
diff --git a/artifacts/monster-realms/src/index.css b/artifacts/monster-realms/src/index.css
index 4b6aa13..deecb55 100644
--- a/artifacts/monster-realms/src/index.css
+++ b/artifacts/monster-realms/src/index.css
@@ -519,6 +519,20 @@
 
   .battle-screen-shell { height: 100dvh; max-height: 100dvh; overflow: hidden; isolation: isolate; }
   .battle-arena { flex: 1 1 0%; min-height: 0; background: #05020a; }
+  .battle-arena::before {
+    content:""; position:absolute; z-index:1; pointer-events:none; left:7%; right:7%; bottom:-15%; height:59%;
+    border-radius:50%; transform:perspective(760px) rotateX(61deg); transform-origin:bottom;
+    background:
+      repeating-radial-gradient(ellipse at 50% 42%,transparent 0 45px,rgba(255,203,143,.085) 47px 48px,transparent 50px 78px),
+      repeating-conic-gradient(from 7deg,transparent 0 11deg,rgba(255,194,122,.065) 11.5deg 12deg,transparent 12.5deg 24deg);
+    border:1px solid rgba(255,176,103,.2); box-shadow:inset 0 0 80px rgba(0,0,0,.45),0 -5px 32px rgba(255,94,34,.12);
+    mask-image:radial-gradient(ellipse,black 18%,rgba(0,0,0,.82) 57%,transparent 78%);
+  }
+  .battle-arena::after {
+    content:""; position:absolute; z-index:1; pointer-events:none; left:12%; right:12%; bottom:50%; height:2px;
+    background:linear-gradient(90deg,transparent,rgba(255,181,113,.14),rgba(255,224,184,.42),rgba(255,181,113,.14),transparent);
+    box-shadow:0 0 18px rgba(255,101,42,.25);
+  }
   .battle-hp-position-left { top: max(14px, env(safe-area-inset-top)); left: 16px; }
   .battle-hp-position-right { top: max(14px, env(safe-area-inset-top)); right: 16px; }
 
@@ -644,7 +658,7 @@
     pointer-events: none;
     background:
       linear-gradient(to bottom, rgba(2,5,16,0.22), transparent 22%, transparent 76%, rgba(2,5,16,0.18)),
-      radial-gradient(ellipse at 50% 58%, transparent 35%, rgba(1,3,12,0.48) 100%);
+      radial-gradient(ellipse at 50% 58%, transparent 45%, rgba(1,3,12,0.24) 100%);
   }
 
   .battle-sky-bloom {
@@ -966,9 +980,10 @@
   /* Snappier lunge — fast dart forward, elastic snap back */
   @keyframes myth-lunge-left {
     0%   { transform: translateX(0) scaleX(1) scaleY(1); }
-    10%  { transform: translateX(8px) scaleX(0.88) scaleY(1.08); }   /* squash-windup */
-    42%  { transform: translateX(-105px) scaleX(1.18) scaleY(0.92); } /* dart */
-    65%  { transform: translateX(-54px) scaleX(0.95) scaleY(1.04); } /* impact recoil */
+    12%  { transform: translateX(18px) scaleX(0.9) scaleY(1.08); }
+    48%  { transform: translateX(clamp(-430px,-34vw,-250px)) scaleX(1.1) scaleY(.96); }
+    62%  { transform: translateX(clamp(-420px,-33vw,-242px)) scaleX(.98) scaleY(1.03); }
+    76%  { transform: translateX(clamp(-430px,-34vw,-250px)) scaleX(1.04) scaleY(.98); }
     100% { transform: translateX(0) scaleX(1) scaleY(1); }
   }
   .myth-lunge-left {
@@ -977,24 +992,36 @@
 
   @keyframes myth-lunge-right {
     0%   { transform: translateX(0) scaleX(1) scaleY(1); }
-    10%  { transform: translateX(-8px) scaleX(0.88) scaleY(1.08); }
-    42%  { transform: translateX(105px) scaleX(1.18) scaleY(0.92); }
-    65%  { transform: translateX(54px) scaleX(0.95) scaleY(1.04); }
+    12%  { transform: translateX(-18px) scaleX(0.9) scaleY(1.08); }
+    48%  { transform: translateX(clamp(250px,34vw,430px)) scaleX(1.1) scaleY(.96); }
+    62%  { transform: translateX(clamp(242px,33vw,420px)) scaleX(.98) scaleY(1.03); }
+    76%  { transform: translateX(clamp(250px,34vw,430px)) scaleX(1.04) scaleY(.98); }
     100% { transform: translateX(0) scaleX(1) scaleY(1); }
   }
   .myth-lunge-right {
     animation: myth-lunge-right 1.65s cubic-bezier(0.18, 0.72, 0.22, 1) forwards;
   }
 
+  @keyframes myth-cast-left {
+    0%,100%{transform:translateX(0) scale(1)} 18%{transform:translateX(18px) translateY(5px) scale(.94,1.06)}
+    42%{transform:translateX(-24px) translateY(-10px) scale(1.08,.96)} 58%{transform:translateX(-14px) translateY(-5px) scale(1.03)}
+  }
+  @keyframes myth-cast-right {
+    0%,100%{transform:translateX(0) scale(1)} 18%{transform:translateX(-18px) translateY(5px) scale(.94,1.06)}
+    42%{transform:translateX(24px) translateY(-10px) scale(1.08,.96)} 58%{transform:translateX(14px) translateY(-5px) scale(1.03)}
+  }
+  .myth-cast-left{animation:myth-cast-left 1.65s cubic-bezier(.18,.72,.22,1) forwards}
+  .myth-cast-right{animation:myth-cast-right 1.65s cubic-bezier(.18,.72,.22,1) forwards}
+
   /* Turn camera: a restrained virtual dolly that keeps the acting Myth readable. */
   .battle-turn-camera-focus {
     position: absolute; inset: 0; z-index: 10; pointer-events: none;
     transition: background 1.1s ease, opacity .8s ease;
-    background: radial-gradient(ellipse 42% 66% at 78% 62%, transparent 12%, rgba(2,2,10,.14) 58%, rgba(2,2,10,.46) 100%);
-    mix-blend-mode: multiply;
+    background: radial-gradient(ellipse 34% 54% at 78% 62%, rgba(255,255,255,.055), transparent 68%);
+    mix-blend-mode: screen;
   }
   .battle-focus-wild .battle-turn-camera-focus {
-    background: radial-gradient(ellipse 42% 66% at 22% 62%, transparent 12%, rgba(2,2,10,.14) 58%, rgba(2,2,10,.46) 100%);
+    background: radial-gradient(ellipse 34% 54% at 22% 62%, rgba(255,255,255,.055), transparent 68%);
   }
   .battle-combatant-player, .battle-combatant-wild {
     transition: transform 1.05s cubic-bezier(.2,.72,.24,1), filter .8s ease, opacity .8s ease;
@@ -1002,17 +1029,20 @@
   }
   .battle-focus-player .battle-combatant-player,
   .battle-focus-wild .battle-combatant-wild {
-    transform: scale(1.055) translateY(-4px);
-    filter: saturate(1.08) brightness(1.04);
+    transform: scale(1.13) translateY(-8px);
+    filter: saturate(1.14) brightness(1.08) drop-shadow(0 14px 28px rgba(0,0,0,.42));
   }
   .battle-focus-player .battle-combatant-wild,
-  .battle-focus-wild .battle-combatant-player { opacity: .9; filter: brightness(.88) saturate(.9); }
-  .battle-focus-cinematic .battle-turn-camera-focus { opacity: .82; }
+  .battle-focus-wild .battle-combatant-player { opacity: 1; filter: brightness(1) saturate(1); }
+  .battle-focus-cinematic .battle-turn-camera-focus { opacity: .55; }
   .battle-focus-cinematic.battle-focus-player .battle-combatant-player,
   .battle-focus-cinematic.battle-focus-wild .battle-combatant-wild {
-    transform: scale(1.11) translateY(-9px);
-    filter: saturate(1.16) brightness(1.08) drop-shadow(0 12px 24px rgba(0,0,0,.38));
+    transform: scale(1.1) translateY(-7px);
+    filter: saturate(1.15) brightness(1.08) drop-shadow(0 12px 24px rgba(0,0,0,.38));
   }
+  .battle-focus-cinematic .battle-combatant-player,
+  .battle-focus-cinematic .battle-combatant-wild { opacity:1; filter:brightness(1.06) saturate(1.1); }
+  .battle-action-melee .battle-stage-pool { opacity:.9; filter:blur(1px) brightness(1.16); }
 
   /* ── Floating damage number ─────────────────────────────────────────── */
   @keyframes dmg-float {
-- 
2.51.1

